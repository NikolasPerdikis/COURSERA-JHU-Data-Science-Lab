# Keep here reuseable code snippets 
east <- subset(pollution, region == "east")
table(mpg$model) #shows how many models are in the dataset

#convert a string to a date
as.date(as.character(<integer field YYYYMMDD>),"%Y%m%d")
